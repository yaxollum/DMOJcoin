#include <iostream>

using namespace std;

int main() 
{
    int n;
    cin>>n;
    long long num1, num2, num3;
    for (int loop=0;loop<n;++loop)
    {
        cin>>num1;
        cin>>num2;
        cin>>num3;
        
        cout<<((num1*num2!=num3)?"16 BIT S/W ONLY":"POSSIBLE DOUBLE SIGMA")<<endl;
    }
    return 0;
}