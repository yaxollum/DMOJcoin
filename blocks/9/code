#include <iostream>
#include <string>

using namespace std;

int main()
{
	string dat;
	getline(cin, dat);

	bool change = false;
	auto it = dat.begin();
	while (*it == 'n')
	{
		change = !change;
		it += 4;
	}

	bool last;
	if (*it == 'T') last = true;
	else last = false;

	if (change) last = (!last);

	cout << (last ? "True" : "False") << endl;

	return 0;
}