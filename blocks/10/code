#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
    int n;
    cin>>n;
    vector<int> nn(n);

    for(int i=0;i<n;++i)
    {
        cin>>nn[i];
    }
    
    sort(nn.begin(),nn.end());
    
    for (int i=0;i<n;++i)
    {
        cout<<nn[i]<<endl;
    }

	return 0;
}